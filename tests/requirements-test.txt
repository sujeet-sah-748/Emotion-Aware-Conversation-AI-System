# ===========================================================================
# Test dependencies for emotion-chatbot backend
# Install with: pip install -r requirements-test.txt
# ===========================================================================

# Core test framework
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-timeout==2.3.1
pytest-mock==3.14.0

# FastAPI test client
httpx==0.27.2

# Mocking
# (unittest.mock is stdlib; listed here for clarity)
# responses==0.25.3    # optional: if switching to responses-based HTTP mocking

# Code quality (run separately; not part of pytest)
ruff==0.6.9
mypy==1.11.2

# Coverage reporting
coverage[toml]==7.6.1
